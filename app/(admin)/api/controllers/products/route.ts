import { Client, Databases } from "node-appwrite";
import { NextRequest } from "next/server";
import { Product } from "@/lib";





async function createProduct({product}: {product: Product}){
  try {

  } catch (error) {

  }
}
